todo:

  send on enter
  usernames
    from form field or from devise
    ?maybe with list of logged in users
      (send pusher a message when user joins)
  change title on new message
    flash a * for one second
      look for setinterval
  put time of chat in hover
  make it go ping

  urlencode message
    and html escape it
    (or just switch to post)
